{#----------------------------------------------------------------------------------------------------------------------#}
{#---------------------------------                AFFICHAGE D'UN ARTICLE            -----------------------------------#}
{#----------------------------------------------------------------------------------------------------------------------#}
{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}


{# Ici j'utilise un tableau pour l'affichage d'un article en particulier. Mais cette fonction n'est pas encore utilisé à son potentiel maximum. #}
{# Je dois d'abord valider la V1 avec la cliente pour passer à la suite du dev. Mais le fichier me servira à afficher les documents partagés dans
 l'espace utilisateur. Il sera amené à être modifier évidemment. #}
{% block body %}
    <h1>Article</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id de l'article</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Paragrape de l'article</th>
                <td>{{ article.text|raw }}</td>
            </tr>
            <tr>
                <th>Image de l'article</th>
                <td><img src="{{ asset('assets/uploaded_images/') ~ article.filename }}" alt=""></td>
            </tr>
        </tbody>
    </table>
    <a class="buttonadmin" href="{{ path('article_edit', {'id': article.id}) }}">Éditer</a>
    <br>
    {{ include('article/_delete_form.html.twig') }}
    <a class="buttonadmin" href="{{ path('article_index') }}">Retour</a>
{% endblock %}
